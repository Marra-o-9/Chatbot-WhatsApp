services:
  - name: chatbot_whatsapp
    type: web
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python chatbot_whatsapp.py"
    envVars:
      - key: OPENAI_API_KEY
        sync: true
